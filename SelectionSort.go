package Sort

// A pointer from left to right.
// All element at the left of the pointer are sorted
func SelectionSort(values []Comparable) {

	for i := 0; i < len(values); i++ {
		for j := 0; j < i; j++ {
			// TODO
		}
	}
}
func Exchange(values []Comparable, elem1, elem2 int) {
	if less(values[elem1] , values[elem2]
}
